# 로컬 개발용 키 — 이 파일을 `.env` 로 복사하고 값을 채운다. `.env` 는 gitignore 됨(커밋 금지).
# 운영(8600) env 와 동일한 키. 없으면 룰+캐시(오프라인)로만 동작한다.

# 장소→좌표(keyword), 좌표→시군구(coord2regioncode) 에 쓰는 REST API 키
KAKAO_REST_KEY=

# /map 카카오 지도 SDK 로드용 JavaScript 키 (REST 키와 다름)
KAKAO_JS_KEY=
