# Python
__pycache__/
*.pyc
.venv/

# 로컬 키 (절대 커밋 금지 — .env.example 만 커밋)
.env

# 학습 산출물 (train_to_tflite.py 실행 시 자동 생성 — 재생성 가능)
models/

# 런타임 DB (app.py 실행 시 생성)
pest_reports.db

# 실행 로그
*.log

# HF Spaces 업로드용 staging (deploy-hf/deploy.py 가 재생성)
.hfspace/
